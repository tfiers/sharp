# Configure the following environment variables:
# - LUIGI_CONFIG_PARSER = toml

[core]
logging_conf_file = "../../logging.cfg"


[worker]
task_process_context = ""  # Luigi bugfix.
keep_alive = false

[execution_summary]
summary_length = 0  # Display _all_ ran tasks.


[Sharp]
config_id = "test"


[Data]
output_dir = "data/processed"
raw_data_dir = "data/raw"
reference_channel = "L2 - E13_extract"


[ThresholdSweeper]
num_thresholds = 6


[NeuralNet]
num_layers = 1
num_units_per_layer = 25
num_epochs = 2
